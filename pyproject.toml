[project]
name = "chatbot-sandbox"
version = "0.1.0"
description = "A simple AI chatbot built with FastAPI and OpenAI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "openai>=1.50.0",
    "python-dotenv>=1.0.0",
]
